{
  "name": "resolver",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm --filter website build && pnpm --filter resolver-manage-system build",
    "start": "node apps/backend/server.js",
    "dev:backend": "nodemon apps/backend/server.js",
    "lint": "pnpm -r run lint"
  }
}
