# Backend (apps/backend)
PORT=5173
MONGODB_URI=mongodb://127.0.0.1:27017/resolver
JWT_SECRET=change-me-in-production
# OPENAI_API_KEY=
# Incident / AI automation — add providers as wired

# Frontends — base URL for REST + Socket.IO client
VITE_API_URL=http://localhost:5173
